#lang racket
(require rackunit)
(require "./helpers.rkt")

(define (square-list items)
  (if (null? items)
      null
      (cons (square (car items)) (square-list (cdr items)))))

(check-equal? (square-list (list 1 2 3)) (list 1 2 3))